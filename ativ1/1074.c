#include <stdio.h>

int main(void){
    int qtdEntrada;
    int entradas[qtdEntrada];
    scanf("%d",qtdEntrada);
    for(i=0;i<qtdEntrada;i++){
        scanf("%d",entradas[i]);
    }
    prinf("%d",entradas);

	return 0;
}